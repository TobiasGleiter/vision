export default class Graph {
    constructor(nodes) {
        this.nodes = nodes;
        this.selectedNode = null;
        this.dragging = false;

    }

    test() {
        console.log("Test")
    }
}